---
phase: 01-build-pipeline-signing
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: false
requirements: [DIST-03]
must_haves:
  truths:
    - "O APK dist/MicAlternativo-v0.1.0.apk instala por sideload no Samsung A15 5G real (Android 14 / One UI 6) e abre mostrando 'MicAlternativo' e 'v0.1.0' (Success Criterion 3 da fase)"
    - "O APK dist/MicAlternativo-v0.1.1.apk instala POR CIMA do 0.1.0 sem desinstalar (update aceito — mesma assinatura) e abre mostrando 'v0.1.1' (DIST-03, Success Criterion 4)"
    - "Antes da transferência, ambos os APKs re-passam os gates e têm sha256 impresso para conferência de integridade"
  artifacts:
    - path: "dist/MicAlternativo-v0.1.0.apk"
      provides: "Build #1 (versionCode 1) — produzido pelo plano 01-01, verificado aqui antes da transferência"
    - path: "dist/MicAlternativo-v0.1.1.apk"
      provides: "Build #2 (versionCode 2, mesmo certificado) — prova o caminho de update no aparelho"
  key_links:
    - from: "dist/MicAlternativo-v0.1.0.apk + dist/MicAlternativo-v0.1.1.apk"
      to: "Samsung A15 5G (aparelho físico)"
      via: "transferência manual (USB/Drive) ou adb install -r — nenhum device conectado neste ambiente (RESEARCH Pitfall 8), por isso checkpoint:human-verify"
      pattern: "MicAlternativo-v0\\.1\\."
---

<objective>
Fechar o Success Criterion 3 da fase: provar no aparelho real (Samsung A15 5G, Android 14 / One UI 6) que o APK produzido pelo pipeline instala por sideload, abre, e que a versão seguinte atualiza POR CIMA sem desinstalar (DIST-03 — mesma keystore permanente).

Nenhum device está conectado neste ambiente (verificado na pesquisa — Pitfall 8), então a instalação é necessariamente um checkpoint humano: os gates automáticos do plano 01-01 provam a *prontidão* de instalação; este plano prova a instalação em si.

Purpose: sem esta prova, todo o loop de teste das fases 2–4 ("compilar → instalar → testar no aparelho") fica sobre uma suposição.
Output: confirmação humana registrada; nenhum arquivo novo (plano somente leitura).
</objective>

## Phase Goal

**Goal (ROADMAP):** A signed, installable "Hello World" APK is produced by one script, with automated gates that guarantee it installs on Android 14

> Nota: linha `**Goal:**` do ROADMAP não está em formato de user story (ver nota no plano 01-01). Escopo definido pelos Success Criteria + D-01..D-08.

## Artifacts This Phase Produces

Este plano não cria nem modifica arquivos. Consome os artefatos do plano 01-01: `dist/MicAlternativo-v0.1.0.apk`, `dist/MicAlternativo-v0.1.1.apk` e o modo `scripts/build.sh --verify-only`.

<execution_context>
@/home/temp/.claude/gsd-core/workflows/execute-plan.md
@/home/temp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline-signing/01-RESEARCH.md
@.planning/phases/01-build-pipeline-signing/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verificar artefatos e imprimir instruções de transferência</name>
  <files>dist/MicAlternativo-v0.1.0.apk, dist/MicAlternativo-v0.1.1.apk (somente leitura — nenhum arquivo criado ou modificado)</files>
  <read_first>
    - .planning/phases/01-build-pipeline-signing/01-01-SUMMARY.md — estado final do plano 01-01 (versões, digests)
    - .planning/phases/01-build-pipeline-signing/01-RESEARCH.md — Pitfall 8 (sem device conectado; adb como opção quando houver) e "Assumptions Log" B1
  </read_first>
  <action>
    Preparar o pacote de verificação humana (sem tocar nos APKs — qualquer modificação pós-assinatura invalida a assinatura):

    1. Rodar `scripts/build.sh --verify-only dist/MicAlternativo-v0.1.0.apk` e `scripts/build.sh --verify-only dist/MicAlternativo-v0.1.1.apk` — ambos devem sair com exit 0 (gates no artefato exato que será transferido).
    2. Imprimir bloco PT-BR para o usuário com: caminho absoluto de cada APK, tamanho (`stat -c %s`), `sha256sum` de cada um (conferência de integridade pós-transferência, se desejada), e as duas rotas de instalação:
       - Rota A (sem cabo): enviar os dois APKs para o aparelho (Google Drive, e-mail para si mesmo, ou copiar por cabo USB no modo transferência de arquivos) e instalar tocando no arquivo no app "Meus Arquivos".
       - Rota B (com adb, se depuração USB ativa): `"$ANDROID_HOME/platform-tools/adb" install -r dist/MicAlternativo-v0.1.0.apk` e depois o mesmo comando com o 0.1.1 (o `-r` faz o update-install).
    3. Checar `adb devices` uma vez: se algum device aparecer (improvável — nenhum conectado na pesquisa), informar no bloco que a Rota B está disponível agora.
  </action>
  <verify>
    <automated>scripts/build.sh --verify-only dist/MicAlternativo-v0.1.0.apk && scripts/build.sh --verify-only dist/MicAlternativo-v0.1.1.apk && sha256sum dist/MicAlternativo-v0.1.0.apk dist/MicAlternativo-v0.1.1.apk</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build.sh --verify-only` exit 0 para os dois APKs imediatamente antes do checkpoint
    - Saída do task contém o sha256 e o tamanho de cada APK e instruções PT-BR das rotas A e B
    - Nenhum arquivo em dist/ foi modificado (mtime/sha256 inalterados após o task)
  </acceptance_criteria>
  <done>Ambos os APKs re-verificados no estado exato de transferência; usuário tem instruções PT-BR completas e hashes para conferência.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Instalar e atualizar o app no Samsung A15 5G (prova real de SC3 + DIST-03)</name>
  <files>nenhum — verificação humana no aparelho físico; dist/ intocado</files>
  <read_first>
    - Saída do Task 1 deste plano (hashes, caminhos e rotas de instalação)
  </read_first>
  <action>Pausar a execução e apresentar ao usuário o bloco how-to-verify abaixo, junto com os hashes/caminhos impressos pelo Task 1. Não automatizar nada aqui: não há device conectado neste ambiente (RESEARCH Pitfall 8) e a instalação por sideload no aparelho físico é, por definição, a única prova válida do Success Criterion 3. Aguardar o resume-signal antes de concluir o plano.</action>
  <what-built>
    Pipeline completo de build sem Gradle (plano 01-01) que produziu dois APKs assinados com a MESMA keystore permanente do projeto:
    - `dist/MicAlternativo-v0.1.0.apk` (versionCode 1)
    - `dist/MicAlternativo-v0.1.1.apk` (versionCode 2)
    Todos os gates automáticos passam (assinatura v3, alinhamento, badging). Falta a única prova que máquina nenhuma pode dar aqui: instalar no seu aparelho de verdade.
  </what-built>
  <how-to-verify>
    No seu Samsung Galaxy A15 5G (Android 14 / One UI 6):

    1. Transfira os DOIS arquivos APK de `dist/` para o celular (Google Drive, e-mail, ou cabo USB), conforme as instruções impressas no passo anterior. Se preferir e a depuração USB estiver ativa, use os comandos adb da Rota B.
    2. Instale primeiro o `MicAlternativo-v0.1.0.apk`: toque no arquivo no app "Meus Arquivos" (ou "Files"). Se o Android pedir, permita "Instalar apps desconhecidos" para o app que você usou para abrir o arquivo. Se aparecer aviso do Play Protect ("App não verificado" / "Verificação do app"), toque em "Instalar mesmo assim" — esse aviso é esperado para app fora da Play Store (o guia completo de sideload, incluindo o Auto Blocker da Samsung, é escopo da Fase 5).
    3. Abra o app "MicAlternativo" na gaveta de apps. CONFIRME: ele abre sem travar e mostra o texto "MicAlternativo" e, abaixo, "v0.1.0".
    4. SEM desinstalar nada, instale agora o `MicAlternativo-v0.1.1.apk` por cima (mesmo processo do passo 2). CONFIRME: a instalação conclui como atualização, SEM erro do tipo "App não instalado" ou "pacote conflita com um pacote existente" — se esse erro aparecer, a assinatura divergiu e isso é uma FALHA (descreva a mensagem exata).
    5. Abra o app de novo. CONFIRME: agora mostra "v0.1.1".
  </how-to-verify>
  <acceptance_criteria>
    - Passo 3 confirmado: v0.1.0 instala por sideload e abre exibindo "MicAlternativo" + "v0.1.0" (Success Criterion 3)
    - Passo 4 confirmado: v0.1.1 instala POR CIMA sem desinstalar e sem erro de assinatura (DIST-03 / Success Criterion 4)
    - Passo 5 confirmado: app aberto exibe "v0.1.1"
  </acceptance_criteria>
  <verify>
    <human-check>Usuário confirma no A15 5G: passo 3 (v0.1.0 instala e abre), passo 4 (v0.1.1 atualiza por cima sem erro de assinatura) e passo 5 (app exibe v0.1.1).</human-check>
  </verify>
  <done>Usuário respondeu "aprovado" confirmando os passos 3, 4 e 5 — SC3 e DIST-03 provados no aparelho real; ou reportou falha com a mensagem exata do instalador (vira gap para replanejamento).</done>
  <resume-signal>Digite "aprovado" se os passos 3, 4 e 5 funcionaram; caso contrário, descreva o que falhou (mensagem de erro exata do instalador, se houver).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist/*.apk → canal de transferência (Drive/e-mail/USB) → aparelho | Artefato assinado atravessa canal não controlado até o A15 5G |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | APK em trânsito (Drive/e-mail/USB) | low | mitigate | Assinatura v3 cobre o arquivo inteiro — APK adulterado é REJEITADO pelo instalador do Android; adicionalmente Task 1 imprime sha256 para conferência manual opcional |
| T-01-08 | Spoofing | instalar um APK errado/antigo por engano | low | mitigate | Task 1 lista caminho + tamanho + sha256 de cada versão; passos 3 e 5 do checkpoint verificam a versão exibida NO app |
| T-01-SC | Tampering | supply chain de pacotes | low | accept | Nenhum pacote instalado neste plano (somente leitura + verificação humana) |
</threat_model>

<verification>
- `scripts/build.sh --verify-only` exit 0 nos dois APKs no momento da transferência
- Confirmação humana: instala (SC3), atualiza por cima (SC4/DIST-03), abre exibindo a versão correta
</verification>

<success_criteria>
- Usuário confirmou no A15 5G real: sideload do v0.1.0 OK, app abre; update-install do v0.1.1 por cima OK sem desinstalar; versão exibida atualiza
- Qualquer falha reportada vira gap com a mensagem exata do instalador (para diagnóstico de assinatura/esquema)
</success_criteria>

<output>
Create `.planning/phases/01-build-pipeline-signing/01-02-SUMMARY.md` when done
</output>

---
phase: 01-build-pipeline-signing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - version.properties
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/br/com/micalternativo/MainActivity.java
  - scripts/build.sh
  - scripts/test_build.sh
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]
must_haves:
  truths:
    - "Um único comando (scripts/build.sh) produz dist/MicAlternativo-v{versionName}.apk assinado, sem nenhum passo manual (DIST-01, D-03)"
    - "Se apksigner verify ou zipalign -c falharem, o build termina com exit != 0 e mensagem clara — nunca produz APK quebrado silenciosamente (DIST-02, D-08)"
    - "O gate de assinatura aceita exit 0 E (v2 true OU v3 true) — minSdk 29 emite v3-only por design; um gate só-v2 falharia todo build válido (RESEARCH Pitfall 1)"
    - "Dois builds consecutivos são assinados com o mesmo certificado (digest SHA-256 idêntico: b6966a76565821025c2ed22ea8325e4108c9beff70686baacb5dee496b8d6fe8), permitindo update-install (DIST-03, D-01)"
    - "aapt2 dump badging do artefato final mostra package br.com.micalternativo, sdkVersion 29, targetSdkVersion 34, RECORD_AUDIO e launchable-activity MainActivity (D-05, D-06)"
    - "A senha da keystore nunca aparece em argv nem no repo — apksigner usa --ks-pass env:STOREPASS após source de keystore.env (D-01, D-02)"
  artifacts:
    - path: "scripts/build.sh"
      provides: "Pipeline completo aapt2 link -> javac -> d8 -> zip -j -> zipalign -> apksigner + gates + summary + modo --verify-only"
      min_lines: 100
    - path: "scripts/test_build.sh"
      provides: "Harness E2E independente: roda build.sh e re-verifica gates, badging e digest do certificado por fora"
      min_lines: 40
    - path: "version.properties"
      provides: "Fonte única de versionCode/versionName injetados via aapt2 link (D-04)"
      contains: "versionCode="
    - path: "app/src/main/AndroidManifest.xml"
      provides: "Manifest final: minSdk 29, targetSdk 34, RECORD_AUDIO, label MicAlternativo, MainActivity exported (D-06)"
      contains: "br.com.micalternativo"
    - path: "app/src/main/java/br/com/micalternativo/MainActivity.java"
      provides: "Activity esqueleto com UI 100% programática, sem AndroidX (D-05)"
      contains: "extends Activity"
  key_links:
    - from: "version.properties"
      to: "scripts/build.sh"
      via: "parse CRLF-safe (grep/cut/tr) injetado em aapt2 link --version-code/--version-name — nunca editar versão no manifest à mão"
      pattern: "version-code"
    - from: "/home/temp/.micalternativo-keys/keystore.env"
      to: "scripts/build.sh"
      via: "set -a; source; set +a e assinatura com --ks-pass env:STOREPASS (senha fora do argv)"
      pattern: "env:STOREPASS"
    - from: "scripts/test_build.sh"
      to: "scripts/build.sh"
      via: "harness invoca scripts/build.sh e depois re-executa apksigner verify + zipalign -c de forma independente no artefato final"
      pattern: "scripts/build.sh"
    - from: "scripts/build.sh (stage zipalign)"
      to: "scripts/build.sh (stage apksigner sign)"
      via: "zipalign -f -p 4 SEMPRE antes de apksigner sign; nada modifica o APK após a assinatura (D-07)"
      pattern: "zipalign"
---

<objective>
Walking Skeleton do MicAlternativo: o menor stack completo que prova o pipeline inteiro — código-fonte Java + manifest viram um APK assinado e instalável em UM comando (`scripts/build.sh`), com gates automáticos que impedem qualquer APK quebrado de ser produzido, assinado sempre com a keystore permanente do projeto.

Implementa as decisões travadas D-01 a D-08 do CONTEXT.md e os requisitos DIST-01 (build reproduzível sem Gradle), DIST-02 (gates apksigner verify + zipalign -c) e DIST-03 (keystore permanente, mesma assinatura em todos os builds).

Purpose: todas as fases 2–5 dependem deste loop "compilar → assinar → instalar" confiável; é a maior incerteza de infra do projeto e por isso vem primeiro.
Output: `scripts/build.sh`, `scripts/test_build.sh`, `version.properties`, manifest e MainActivity definitivos; `dist/MicAlternativo-v0.1.0.apk` e `dist/MicAlternativo-v0.1.1.apk` (mesmo certificado) prontos para o checkpoint humano do plano 01-02.
</objective>

## Phase Goal

**Goal (ROADMAP):** A signed, installable "Hello World" APK is produced by one script, with automated gates that guarantee it installs on Android 14

> Nota: a linha `**Goal:**` do ROADMAP não está no formato canônico de user story ("As a / I want to / so that"), embora a fase esteja em `Mode: mvp`. Nenhuma story foi inventada — o escopo está integralmente definido pelos Success Criteria da fase + decisões D-01..D-08. Para normalizar o formato, rode `/gsd mvp-phase 1`.

## Artifacts This Phase Produces

Arquivos (committáveis):
- `scripts/build.sh` — o sistema de build inteiro. Funções: `die()` (erro PT-BR + exit 1), `preflight()` (resolve ANDROID_HOME/BT/ANDROID_JAR/KEYSTORE_PATH/KEYSTORE_ENV e verifica cada ferramenta), `parse_version()` (leitura CRLF-safe de version.properties), `stage_link()`, `stage_compile()`, `stage_dex()`, `stage_pack()`, `stage_align()`, `stage_sign()`, `run_gates()` (gate 1 apksigner verify + regex v2|v3, gate 2 zipalign -c), `print_summary()`, `main()`. Modo CLI extra: `scripts/build.sh --verify-only <apk>` (só preflight de ferramentas + gates).
- `scripts/test_build.sh` — harness E2E independente (RED no Task 1, GREEN no Task 2).
- `version.properties` — `versionCode` / `versionName` (fonte única de versão, D-04).
- `app/src/main/AndroidManifest.xml` — forma final (D-06), nunca mais editado nas fases 2–4 exceto para novos componentes.
- `app/src/main/java/br/com/micalternativo/MainActivity.java` — classe `br.com.micalternativo.MainActivity` (D-05).

Artefatos gerados (gitignored): `build/` (base.apk, obj/, dex/, unsigned.apk, aligned.apk), `dist/MicAlternativo-v0.1.0.apk`, `dist/MicAlternativo-v0.1.1.apk`.

<execution_context>
@/home/temp/.claude/gsd-core/workflows/execute-plan.md
@/home/temp/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline-signing/01-CONTEXT.md
@.planning/phases/01-build-pipeline-signing/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar esqueleto do app + harness E2E scripts/test_build.sh (RED)</name>
  <files>version.properties, app/src/main/AndroidManifest.xml, app/src/main/java/br/com/micalternativo/MainActivity.java, scripts/test_build.sh</files>
  <read_first>
    - .planning/phases/01-build-pipeline-signing/01-RESEARCH.md — seções "Code Examples" (Q7 parse de versão, Q8 MainActivity, bloco "Manifest") e "Common Pitfalls" (Pitfall 1 esquema v3, Pitfall 3 pipes, Pitfall 7 CRLF)
    - .planning/phases/01-build-pipeline-signing/01-CONTEXT.md — decisões D-04, D-05, D-06
  </read_first>
  <action>
    Criar os quatro arquivos-fonte do esqueleto. Todo o conteúdo Java/XML já foi validado empiricamente na pesquisa — transcrever, não recriar.

    1. `version.properties` na raiz (D-04), LF puro (sem CRLF), exatamente duas linhas de dados: `versionCode=1` e `versionName=0.1.0`.

    2. `app/src/main/AndroidManifest.xml` — transcrever o manifest verificado da seção "Manifest" do 01-RESEARCH.md (D-06): package `br.com.micalternativo`, `<uses-sdk android:minSdkVersion="29" android:targetSdkVersion="34"/>`, `<uses-permission android:name="android.permission.RECORD_AUDIO"/>` (fases 2+ usam, declarado desde já), `<application android:label="MicAlternativo">` com `<activity android:name=".MainActivity" android:exported="true">` + intent-filter MAIN/LAUNCHER. `android:exported="true"` é obrigatório em targetSdk 31+ para activity com intent-filter — omitir falha na instalação. Sem ícone próprio (default do sistema — ícone é ideia adiada, fora do escopo).

    3. `app/src/main/java/br/com/micalternativo/MainActivity.java` — transcrever o Q8 do 01-RESEARCH.md (D-05): `package br.com.micalternativo`, `extends android.app.Activity`, UI 100% programática (LinearLayout vertical centrado + TextView "MicAlternativo" 24sp + TextView "v" + versionName lido de `getPackageManager().getPackageInfo(getPackageName(), 0).versionName` com try/catch). Proibido: qualquer biblioteca Jetpack/compat externa (todo import deve vir de `android.*` da plataforma ou `java.*`), XML de layout, R.layout.

    4. `scripts/test_build.sh` (chmod +x) — harness E2E com `set -euo pipefail` que define o contrato do build (é o teste que o Task 2 fará passar). Comportamento, nesta ordem:
       a. Resolver `ANDROID_HOME="${ANDROID_HOME:-/tmp/claude-1000/-home-temp-problem/baf2ba44-b642-4c2a-955d-a883998e26e3/scratchpad/android-sdk}"`, `BT="$ANDROID_HOME/build-tools/34.0.0"` (mesmo default do build.sh, D-03).
       b. Se `scripts/build.sh` não existir ou não for executável: mensagem PT-BR clara + exit 1 (isto é o estado RED deste task).
       c. Rodar `scripts/build.sh`; se exit != 0, falhar.
       d. Parse CRLF-safe de `versionName` em version.properties (padrão Q7: `grep -E '^versionName=' | head -1 | cut -d= -f2 | tr -d '[:space:]'`); montar `APK=dist/MicAlternativo-v${VERSION_NAME}.apk`; falhar se o arquivo não existir.
       e. Gate independente 1: `verify_out=$("$BT/apksigner" verify --verbose --min-sdk-version 29 "$APK")` — invocação capturada em variável, NUNCA em pipe com head/tail (Pitfall 3: o pipe reporta o exit do último comando, engolindo a falha do apksigner). Depois `echo "$verify_out" | grep -Eq '(v2 scheme \(APK Signature Scheme v2\): true|v3 scheme \(APK Signature Scheme v3\): true)'` — aceitar v2 OU v3 (Pitfall 1: minSdk 29 emite v3-only por design; exigir só v2 reprovaria todo build válido).
       f. Gate independente 2: `"$BT/zipalign" -c 4 "$APK"` (invocação bare — exit code direto).
       g. Badging: capturar `"$BT/aapt2" dump badging "$APK"` e exigir presença de: `name='br.com.micalternativo'`, `sdkVersion:'29'`, `targetSdkVersion:'34'`, `android.permission.RECORD_AUDIO`, `launchable-activity: name='br.com.micalternativo.MainActivity'`.
       h. Certificado (DIST-03): capturar `"$BT/apksigner" verify --print-certs "$APK"` e exigir presença do digest `b6966a76565821025c2ed22ea8325e4108c9beff70686baacb5dee496b8d6fe8` (certificado permanente do projeto, verificado na pesquisa).
       i. Sucesso: imprimir `TEST OK: $APK` e exit 0.
  </action>
  <verify>
    <automated>bash -n scripts/test_build.sh && mkdir -p build/check-obj && AH="${ANDROID_HOME:-/tmp/claude-1000/-home-temp-problem/baf2ba44-b642-4c2a-955d-a883998e26e3/scratchpad/android-sdk}" && "$AH/build-tools/34.0.0/aapt2" link --manifest app/src/main/AndroidManifest.xml -I "$AH/platforms/android-34/android.jar" --version-code 1 --version-name 0.1.0 -o build/check-base.apk && javac -source 8 -target 8 -bootclasspath "$AH/platforms/android-34/android.jar" -classpath "$AH/platforms/android-34/android.jar" -encoding UTF-8 -Xlint:-options -d build/check-obj app/src/main/java/br/com/micalternativo/MainActivity.java && if bash scripts/test_build.sh; then echo 'ERRO: harness deveria falhar (RED) sem scripts/build.sh'; exit 1; else echo 'RED confirmado + fontes compilam/linkam'; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^versionCode=1$' version.properties` retorna 1 e `grep -c '^versionName=0.1.0$' version.properties` retorna 1
    - `aapt2 link` do manifest com `-I android.jar` termina com exit 0 (manifest válido, D-06)
    - `javac -source 8 -target 8 -bootclasspath android.jar` compila MainActivity.java com exit 0 (D-05, sem AndroidX)
    - `grep -c 'androidx' app/src/main/java/br/com/micalternativo/MainActivity.java` retorna 0
    - `grep -q 'android:exported="true"' app/src/main/AndroidManifest.xml` passa
    - `test -x scripts/test_build.sh` passa; `bash -n scripts/test_build.sh` exit 0
    - `bash scripts/test_build.sh` termina com exit != 0 enquanto scripts/build.sh não existe (RED), imprimindo mensagem PT-BR citando scripts/build.sh
    - `grep -q 'v3 scheme' scripts/test_build.sh` passa (gate aceita v3, não só v2 — RESEARCH Pitfall 1)
  </acceptance_criteria>
  <done>Fontes do esqueleto criadas e comprovadamente compiláveis/linkáveis com a toolchain real; harness E2E existe, é sintaticamente válido e está RED (falha porque scripts/build.sh ainda não existe).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar scripts/build.sh — pipeline completo com gates (GREEN)</name>
  <files>scripts/build.sh</files>
  <read_first>
    - .planning/phases/01-build-pipeline-signing/01-RESEARCH.md — "System Architecture Diagram", "Code Examples" Q1–Q6 (comandos copy-ready, todos executados com sucesso na pesquisa), "Common Pitfalls" 1–7, "Architecture Patterns" 1–3
    - .planning/phases/01-build-pipeline-signing/01-CONTEXT.md — decisões D-01, D-02, D-03, D-04, D-07, D-08
    - scripts/test_build.sh (criado no Task 1) — é o contrato que este task deve satisfazer
  </read_first>
  <behavior>
    - Test 1 (GREEN): `bash scripts/test_build.sh` passa a terminar com exit 0 — build completo + gates independentes + badging + digest do certificado
    - Test 2 (gate negativo): `scripts/build.sh --verify-only <apk adulterado pós-assinatura>` termina com exit != 0 (assinatura v2/v3 cobre o layout exato de bytes; qualquer modificação invalida)
    - Test 3 (preflight negativo): `KEYSTORE_ENV=/caminho/inexistente.env scripts/build.sh` termina com exit != 0 com mensagem citando o arquivo ausente
  </behavior>
  <action>
    Criar `scripts/build.sh` (chmod +x) transcrevendo os comandos verificados de Q1–Q6 do 01-RESEARCH.md. Estrutura:

    1. `set -euo pipefail` (D-03). Comentário de cabeçalho PT-BR documentando (D-02): perder a keystore = impossibilidade de publicar updates instaláveis por cima — o certificado é a identidade do app.

    2. Configuração por env com defaults (D-02, D-03): `ANDROID_HOME="${ANDROID_HOME:-/tmp/claude-1000/-home-temp-problem/baf2ba44-b642-4c2a-955d-a883998e26e3/scratchpad/android-sdk}"`, `BT="$ANDROID_HOME/build-tools/34.0.0"`, `ANDROID_JAR="$ANDROID_HOME/platforms/android-34/android.jar"`, `KEYSTORE_PATH="${KEYSTORE_PATH:-/home/temp/.micalternativo-keys/release.jks}"`, `KEYSTORE_ENV="${KEYSTORE_ENV:-/home/temp/.micalternativo-keys/keystore.env}"`.

    3. `preflight()`: para cada um de `$BT/aapt2`, `$BT/d8`, `$BT/zipalign`, `$BT/apksigner`, `$ANDROID_JAR`, `javac`, `zip` — se ausente, `die` nomeando exatamente o que falta e como corrigir (ex.: "defina ANDROID_HOME") (D-03, Pattern 3 — o default do SDK vive em scratchpad de sessão, é a falha mais provável em runs futuros). Checar `KEYSTORE_PATH`/`KEYSTORE_ENV` existem. Se `stat -c %a` da keystore ou do env for mais permissivo que 600, imprimir AVISO (não falhar).

    4. `parse_version()`: padrão Q7 CRLF-safe (`grep -E '^versionCode=' version.properties | head -1 | cut -d= -f2 | tr -d '[:space:]'`); validar `[[ "$VERSION_CODE" =~ ^[0-9]+$ ]]` e `[[ -n "$VERSION_NAME" ]]`, senão `die` (Pitfall 7; ASVS V5 — valores entram no argv do aapt2).

    5. Estágios na ordem FIXA de D-07 (limpar `build/` no início; `mkdir -p build/obj build/dex dist` — d8 NÃO cria o diretório de saída, Pitfall 2):
       - `stage_link()`: Q1 — `"$BT/aapt2" link --manifest app/src/main/AndroidManifest.xml -I "$ANDROID_JAR" --version-code "$VERSION_CODE" --version-name "$VERSION_NAME" -o build/base.apk`. SEM `aapt2 compile` (manifest-only, verificado desnecessário — discricionariedade exercida conforme pesquisa).
       - `stage_compile()`: Q2 — `javac -source 8 -target 8 -bootclasspath "$ANDROID_JAR" -classpath "$ANDROID_JAR" -encoding UTF-8 -Xlint:-options -d build/obj $(find app/src/main/java -name '*.java')`. NUNCA combinar `--release 8` com bootclasspath (erro fatal, Pitfall 4); não usar `-Werror`.
       - `stage_dex()`: Q3 — `"$BT/d8" --release --min-api 29 --lib "$ANDROID_JAR" --output build/dex $(find build/obj -name '*.class')`.
       - `stage_pack()`: Q4 — `cp build/base.apk build/unsigned.apk` e `(cd build/dex && zip -j ../unsigned.apk classes.dex)`. Somente APPEND — jamais reempacotar a árvore com zip recursivo (recomprimiria resources.arsc, que existe mesmo em APK manifest-only e deve ficar Stored — Pitfall 5).
       - `stage_align()`: Q5 — `"$BT/zipalign" -f -p 4 build/unsigned.apk build/aligned.apk`, incondicional, SEMPRE antes da assinatura (D-07, Pitfall 6).
       - `stage_sign()`: Q5 — `set -a; source "$KEYSTORE_ENV"; set +a`; `: "${STOREPASS:?ERRO: STOREPASS ausente}"`; `: "${ALIAS:?ERRO: ALIAS ausente}"`; `"$BT/apksigner" sign --ks "$KEYSTORE_PATH" --ks-key-alias "$ALIAS" --ks-pass env:STOREPASS --min-sdk-version 29 --out "dist/MicAlternativo-v${VERSION_NAME}.apk" build/aligned.apk` (D-01, D-04). A senha usa exclusivamente o formato `env:` — nunca o formato de senha em texto claro no argv (visível em ps) e nunca ecoada em log.

    6. `run_gates(apk)` (D-08, DIST-02) — roda no ARTEFATO FINAL e nada o modifica depois:
       - Gate 1: `verify_out=$("$BT/apksigner" verify --verbose --min-sdk-version 29 "$apk")` — exit code checado diretamente na captura (Pattern 1: nunca encadear o gate em pipe antes de ler o exit; um pipe reportaria o exit do consumidor). Depois `grep -Eq '(v2 scheme \(APK Signature Scheme v2\): true|v3 scheme \(APK Signature Scheme v3\): true)'` sobre `$verify_out` — v3 satisfaz o "v2+" do DIST-02 (Pitfall 1: minSdk 29 ⇒ v3-only por design, mesmo com flag forçando v2). Falha ⇒ mensagem PT-BR "APK sem assinatura v2/v3 — não instalará no Android 11+" + exit 1.
       - Gate 2: `"$BT/zipalign" -c 4 "$apk"` bare; falha ⇒ "ERRO: APK desalinhado" + exit 1.

    7. `print_summary(apk)` (D-08): caminho + tamanho (`stat -c %s`), `sha256sum`, linhas de esquema do `$verify_out`, linha SHA-256 de `"$BT/apksigner" verify --print-certs` (continuidade do certificado, DIST-03), e campos-chave de `"$BT/aapt2" dump badging` (package/version/sdk/launchable-activity).

    8. `main()`: se `$1 == --verify-only`, rodar apenas verificação de ferramentas + `run_gates "$2"` + `print_summary "$2"` (usado pelos testes negativos deste plano e pela Fase 5); senão pipeline completo: preflight → parse_version → estágios → run_gates → print_summary.
  </action>
  <verify>
    <automated>bash scripts/test_build.sh && cp dist/MicAlternativo-v0.1.0.apk build/tampered.apk && printf X >> build/tampered.apk && if scripts/build.sh --verify-only build/tampered.apk; then echo 'ERRO: gate aceitou APK adulterado'; exit 1; fi && if KEYSTORE_ENV=/caminho/inexistente.env scripts/build.sh; then echo 'ERRO: build deveria falhar sem keystore.env'; exit 1; fi && echo 'GREEN + gates negativos OK'</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test_build.sh` termina com exit 0 (GREEN): dist/MicAlternativo-v0.1.0.apk existe, apksigner verify exit 0 com esquema v2 OU v3 true, zipalign -c 4 exit 0, badging e digest de certificado conferem
    - `scripts/build.sh --verify-only build/tampered.apk` (cópia com 1 byte anexado pós-assinatura) termina com exit != 0
    - `KEYSTORE_ENV=/caminho/inexistente.env scripts/build.sh` termina com exit != 0 e a saída cita o arquivo/variável ausente
    - `grep -q -- '--ks-pass env:STOREPASS' scripts/build.sh` passa (senha via ambiente)
    - `grep -c -- '--ks-pass pass:' scripts/build.sh` retorna 0 (senha jamais em argv)
    - `grep -q 'set -euo pipefail' scripts/build.sh` passa (D-03)
    - `grep -q 'zipalign' scripts/build.sh && grep -q 'mkdir -p build/obj build/dex dist' scripts/build.sh` passa (D-07 estágios, Pitfall 2)
    - `git check-ignore dist/MicAlternativo-v0.1.0.apk build/base.apk` exit 0 (artefatos gitignored, D-04)
  </acceptance_criteria>
  <done>scripts/build.sh executa o pipeline completo em um comando e o harness independente passa; gates rejeitam APK adulterado com exit != 0; preflight falha com mensagem clara quando keystore.env não existe; senha nunca em argv.</done>
</task>

<task type="auto">
  <name>Task 3: Provar DIST-03 — segundo build com a mesma keystore (bump de versão)</name>
  <files>version.properties</files>
  <read_first>
    - .planning/phases/01-build-pipeline-signing/01-RESEARCH.md — "Open Questions" item 1 (estratégia install → bump → update-install) e Q6 (digest do certificado)
    - .planning/phases/01-build-pipeline-signing/01-CONTEXT.md — decisões D-01, D-04
  </read_first>
  <action>
    Provar que a identidade de assinatura é permanente (DIST-03, D-01) produzindo o par de APKs que o checkpoint humano do plano 01-02 usará para o teste de update-install:

    1. Garantir que `dist/MicAlternativo-v0.1.0.apk` existe (gerado no Task 2; se ausente, rodar `scripts/build.sh` com version.properties ainda em 1/0.1.0).
    2. Editar `version.properties` para `versionCode=2` e `versionName=0.1.1` (bump de update; D-04 — a versão vive só aqui, o manifest não é tocado). Deixar o arquivo NESTE estado final — 0.1.1 passa a ser a versão corrente do projeto.
    3. Rodar `scripts/build.sh` novamente → produz `dist/MicAlternativo-v0.1.1.apk`. Ambos os APKs permanecem em `dist/`.
    4. Extrair o digest SHA-256 do certificado de CADA APK via `"$BT/apksigner" verify --print-certs` e comparar: devem ser idênticos entre si E iguais a `b6966a76565821025c2ed22ea8325e4108c9beff70686baacb5dee496b8d6fe8` (certificado registrado na pesquisa). Digest igual ⇒ o Android aceita instalar 0.1.1 POR CIMA de 0.1.0 sem desinstalar.
    5. Confirmar via `aapt2 dump badging` que o segundo APK carrega `versionCode='2' versionName='0.1.1'` (injeção de versão funcionando, D-04).
  </action>
  <verify>
    <automated>AH="${ANDROID_HOME:-/tmp/claude-1000/-home-temp-problem/baf2ba44-b642-4c2a-955d-a883998e26e3/scratchpad/android-sdk}"; BT="$AH/build-tools/34.0.0"; V0=dist/MicAlternativo-v0.1.0.apk; V1=dist/MicAlternativo-v0.1.1.apk; test -f "$V0" && test -f "$V1" && scripts/build.sh --verify-only "$V0" && scripts/build.sh --verify-only "$V1" && D0=$("$BT/apksigner" verify --print-certs "$V0" | grep -oE '[0-9a-f]{64}' | head -1) && D1=$("$BT/apksigner" verify --print-certs "$V1" | grep -oE '[0-9a-f]{64}' | head -1) && test "$D0" = "$D1" && test "$D0" = "b6966a76565821025c2ed22ea8325e4108c9beff70686baacb5dee496b8d6fe8" && "$BT/aapt2" dump badging "$V1" | grep -q "versionCode='2' versionName='0.1.1'" && echo 'DIST-03 OK: mesmo certificado nos dois builds'</automated>
  </verify>
  <acceptance_criteria>
    - `dist/MicAlternativo-v0.1.0.apk` e `dist/MicAlternativo-v0.1.1.apk` existem simultaneamente
    - `scripts/build.sh --verify-only` exit 0 para os DOIS APKs (ambos passam os gates)
    - Digest SHA-256 do certificado idêntico nos dois APKs e igual a b6966a76565821025c2ed22ea8325e4108c9beff70686baacb5dee496b8d6fe8
    - `aapt2 dump badging` do 0.1.1 contém versionCode='2' versionName='0.1.1'
    - `grep -c '^versionCode=2$' version.properties` retorna 1 (estado final committado)
    - `bash scripts/test_build.sh` continua exit 0 (harness verifica o artefato da versão corrente 0.1.1)
  </acceptance_criteria>
  <done>Dois APKs de versões consecutivas assinados com o certificado permanente idêntico estão em dist/, prontos para o teste humano de install + update-install do plano 01-02; version.properties reflete a versão corrente 0.1.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keystore/keystore.env → build.sh | Segredos de assinatura entram no processo de build (fora do repo) |
| version.properties → argv do aapt2 | Conteúdo de arquivo editável entra na linha de comando de ferramenta |
| dist/*.apk → aparelho do usuário | Artefato assinado atravessa canais de transferência até o A15 5G |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | stage_sign / lista de processos e histórico do shell | high | mitigate | Senha exclusivamente via `--ks-pass env:STOREPASS` após `source` do keystore.env; nunca em argv, nunca ecoada (RESEARCH Pattern 2, verificado) |
| T-01-02 | Information Disclosure | repo git | high | mitigate | `.gitignore` já exclui `*.jks`/`*.keystore`/`*.apk`/`build/`/`dist/`; build.sh referencia segredos só por caminho externo via env (D-02); acceptance gate `git check-ignore` no Task 2 |
| T-01-03 | Tampering | dist/*.apk pós-assinatura | high | mitigate | Gates rodam no artefato FINAL e nada o modifica depois; teste negativo do Task 2 prova que APK adulterado ⇒ apksigner verify exit != 0 |
| T-01-04 | Spoofing/Tampering | degradação de esquema de assinatura (v1-only) | medium | mitigate | Gate exige v2 OU v3 = true na saída do verify; saída v1-only reprova (RESEARCH Security Domain) |
| T-01-05 | Denial of Service (updates) | perda da keystore permanente | critical | mitigate | D-02: aviso documentado no cabeçalho do build.sh; digest do certificado impresso no summary de todo build para checagem de continuidade; preflight avisa se permissões dos arquivos de segredo forem mais frouxas que 600 |
| T-01-06 | Elevation via input | version.properties → argv aapt2 | low | mitigate | ASVS V5: `parse_version()` valida `^[0-9]+$` no versionCode e não-vazio no versionName antes de qualquer uso (RESEARCH Q7) |
| T-01-SC | Tampering | supply chain de pacotes (npm/pip/cargo) | low | accept | Zero pacotes externos nesta fase (Package Legitimacy Audit do RESEARCH: "Not applicable"); toolchain = SDK/JDK pré-instalados e verificados em disco |
</threat_model>

<verification>
- `bash scripts/test_build.sh` — harness E2E independente exit 0 (pipeline + gates + badging + certificado)
- `scripts/build.sh --verify-only <apk adulterado>` exit != 0 — o build nunca aprova APK quebrado (DIST-02)
- Digest de certificado idêntico entre dist/MicAlternativo-v0.1.0.apk e dist/MicAlternativo-v0.1.1.apk (DIST-03)
- `git status` limpo de artefatos: nenhum .apk/.jks/build//dist/ rastreado
</verification>

<success_criteria>
- Um comando (`scripts/build.sh`) produz APK assinado sem passos manuais (SC1/DIST-01)
- Qualquer falha de `apksigner verify` ou `zipalign -c` derruba o build com exit != 0 e mensagem PT-BR (SC2/DIST-02), aceitando v3 como "v2+"
- Dois builds consecutivos compartilham o certificado permanente do projeto (SC4/DIST-03) — pré-condição do update-install humano no plano 01-02 (SC3)
- Fontes do esqueleto (manifest final D-06, MainActivity D-05, version.properties D-04) committadas e compiláveis
</success_criteria>

<output>
Create `.planning/phases/01-build-pipeline-signing/01-01-SUMMARY.md` when done
</output>
